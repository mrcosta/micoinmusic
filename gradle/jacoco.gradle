jacocoTestReport {
    reports {
        xml.enabled = true
        html.enabled = true
    }
    afterEvaluate {
        classDirectories = files(classDirectories.files.collect {
            fileTree(dir: it,
                    exclude: [
                            '**/Application.class**',
                            'com/booter/interceptors/**',
                            'com/booter/configuration/**'
                    ])
        })
    }
}